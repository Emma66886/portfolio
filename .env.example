# Absolute base URL for metadata (Open Graph / Twitter card image URLs).
# Server-only on purpose — do NOT prefix with NEXT_PUBLIC_.
# See the "Environment" note in README.md.
SITE_URL=https://your-domain.com
