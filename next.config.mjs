/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  allowedDevOrigins: ['3000--main--kinemlabs--emma66886.workspace.kinemlabs.com']
};

export default nextConfig;
